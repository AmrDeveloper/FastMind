<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/darkGrey</item>
        <item name="colorAccent">@color/green</item>
        <item name="cardViewStyle">@style/CardView</item>
    </style>

    <!--Style Theme for every Answer RadioButton-->
    <style name="AnswerRadioButtonTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/purple</item>
        <item name="colorControlActivated">@color/yellow</item>
    </style>

    <!--Style Theme for every Answer RadioButton-->
    <style name="AnswerRadioButtonStyle" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:padding">5dp</item>
        <item name="android:background">@color/black</item>
        <item name="android:textColor">@color/gray</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <!--Style Theme for every Action Button on Settings and Main Activity-->
    <style name="ActionButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">serif</item>
        <item name="android:padding">@dimen/dimen15dp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">@dimen/dimen2dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/darkPurple</item>
    </style>

    <!--Style Theme for Win and lose Alert Dialog-->
    <style name="StateDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!--buttons color-->
        <item name="colorAccent">@color/purple</item>
        <!--title and message color-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!--dialog background-->
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!--Style for dialog animation-->
    <style name="DialogAnimationSlide">
        <item name="android:windowEnterAnimation">@anim/side_left</item>
        <item name="android:windowExitAnimation">@anim/side_right</item>
    </style>
</resources>
